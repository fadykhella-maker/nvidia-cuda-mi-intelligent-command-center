# Ephemeral Kaggle tunnel credentials (change every Kaggle/ngrok restart).
# Never commit a real value here — the dashboard also accepts these as
# manual sidebar/query-param input at runtime, which is the primary path.
KAGGLE_JUPYTER_URL=https://your-tunnel.ngrok-free.dev
KAGGLE_JUPYTER_TOKEN=your-current-kernel-gateway-token
